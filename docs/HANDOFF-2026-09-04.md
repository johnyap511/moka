# MOKA handoff, 4 September 2026

Read this first in a new session. It replaces re-reading old chats.

## Where things run
- Production: https://homemoka.com (and www), GCP VM `moka-staging` 35.215.128.58, project `moka-staging-494606`, zone asia-east2-c.
  Native nginx + php-fpm, app at /var/www/moka, MySQL on the same box. This VM *is* production since 4 Sep 2026; there is no separate staging.
- Deploy: `sudo bash scripts/deploy.sh [--migrate]` on the VM, then `sudo chown -R www-data:www-data bootstrap/cache storage`.
- Code: tag `v2026.09.04-locked`. Repos: johnyap511/moka (origin) and sssamkong-arch/mokamoka (owner's private copy); pushes go to both.
- DNS at GoDaddy (ns39/ns40.domaincontrol.com). Zoho mail (MX x3, SPF, DKIM default._domainkey, DMARC). TLS via certbot, auto-renew.
- Backups: /usr/local/bin/moka-backup nightly 03:30 UTC -> /var/backups/moka (14 d) and gs://moka-db-backups-494606 (30 d). Restore: scripts/import_production.sh <dump>.
- Secrets: DB password only in /var/www/moka/.env (and ~/Documents/moka-credentials.txt on the owner's Mac). MAIL_HOST is Zoho but MAIL_PASSWORD is still empty: guest emails do not send yet.

## Rules the data follows (decided by management)
- eZee is the source of truth. Match a reservation on hotel + RES + folio + unit; a stay is `BookingSplitter::stayChain`.
- eZee's amounts are final: the hourly reconcile (`ezee:auto-assign`) reprices any linked booking whose room charge, cleaning fee or cleaning SST differs, and logs it ("Repriced from EZEE"). Dates never follow silently: review item.
- Cleaning fee = eZee's "Cleaning Fee" line (stored breakdown `ezee_bookings.extra_charges`); a "channel" surcharge line stands in only when the folio has no cleaning line (Agoda). Never add both; never deposits or incidentals. SST(CF) = the tax eZee applied on that line.
- Revenue is by calendar month: one booking row per month (splitByMonth); cleaning on the first segment; channel fee follows the nights; SST 8% on top; Long Term Rental carries SST.
- Channels: PMS, Walk-in, Google, Internet, Booking Engine all report as "Website" at the 8% fee. Monthly Rental and LTR: no fee.
- Extra rooms are company listings (owner user 4475), may hold two guests a night. Never cancel in bulk from a pull; cancellations come from the notification feed (`ezee:notifications`) or eZee's reports.

## Month end
1. Revenue Export (EZEE) with the ten eZee files: filter Action column to anything not "None".
2. Old Bookings export (Bookings page, "By check-in date", Export Excel): the 19-column file the owner's Cowork tools consume. Never change its columns.

## Scheduled jobs (root cron -> Laravel scheduler)
hour:update hourly; ezee:notifications hourly at :10; ezee:auto-assign 06:00 (--from pinned to 2026-08-20, should become rolling 14 days); ezee:sweep-cancelled Mon 05:00; moka-backup 03:30 UTC.

## Open items
- 18 Sep: Sarah Atikah RES4160 (Queensville): G-12-03 piece #68632 extended 10–13 Sep (3 nights, 94.56, total 306.37 = eZee room total minus the F-11-16 piece), F-11-16 piece #68633 guest fixed, RES4160 relinked to #68632. ROOT BUG: create (BookController@store) and edit (BookingController@update) looked up the guest with where('email', null) when phone+email were blank, which matched user #10, attached the booking to it and renamed it, so 3,652 bookings showed the last name keyed (Paulina Nowak). Fixed with Guests::isShared (no email, no phone, >50 bookings): lookups only on entered values, never attach/rename a shared account, eZee-linked bookings get Guests::profileFor. Repair: 953 eZee-linked bookings re-attached to real guests, 13 split pieces given their sibling's guest, user #10 renamed 'Guest (name not recorded)' for the 2,686 hand-keyed rest (names unrecoverable). Cleaning for RES4160 (35.07) sits on the F piece as staff keyed it; ground rule says the unit the guest ended in (G-12-03): Sam to decide.
- 18 Sep: cleaning corrections applied from a fresh eZee pull via acceptEzeeAmounts (logged 'Repriced from EZEE'): RES6537-1..5 + RES6538-1,2 (Forum, 13–14 Sep, 40.00→41.85), RES3796-1..3 (Alinea, 9–12 Sep, 41.85→42.00), RES4134-1,2 (Alinea, 6–11 Sep, 41.85+3.35→45.20+0, same total). NOT touched, on purpose: August rows (RES30861, RES30550-1/-2, RES3943-1/-2: eZee now shows 42 cleaning, Homemoka 0; August is stamped, needs Sam), future stays (RES31359, RES6547, RES31384/31385 tenancies, RES4120/4138/4140), multi-piece cases needing a human look (RES4124/RES4005, RES4094-3/RES4091, RES3946, RES3983), and Bell Suites (pull timed out).
- 18 Sep reconciliation preview (fresh eZee pull, Aug–Oct, piece-aware, scripts /tmp/recon3.php on the VM): Forum 366 linked / 15 differ, Arte+Queensville 98 / 4, Alinea 315 / 12, EkoCheras (1–20 Aug, 11–30 Sep) 260 / 14; Bell Suites pull timed out (eZee throttles big windows; pull ≤3-week windows, one hotel at a time). Of 45 differences: day-use rows (expected), Forum OTA rooms where eZee's before-tax is net of commission (compare with the channel rule, not raw), and ~30 real cleaning differences of RM1–30 where eZee's API itself reports 'Channel (OTA)' before night audit and 'Cleaning Fee (OTA)/CF01' after, and Homemoka keeps whichever it last read (RES6522 = 41.85 vs 61.85; RES6537 group = 40 vs 41.85; RES3796 42 vs 41.85). Root cause: no re-read after a stay leaves the hourly window, and no check that listed cleaning equals TotalExtraCharge. Rules for the reconciliation build: (a) re-read every stay for 7 days after check-out; (b) when listed cleaning ≠ TotalExtraCharge, raise review instead of trusting the list; (c) compare using the channel net/gross rule; (d) sum sibling pieces by folio. eZee audit trail shows NO staff edits on these (all OTA/system postings).
- 18 Sep: FN6781/FN6782 (RES6522, Forum RS-23-13 + RS-35-11, 6–7 Sep): eZee's folio showed Cleaning Fee (OTA) 61.85+4.95 SST = 66.80 but Homemoka held an older 'Channel (OTA)' 41.85 line. The charge was edited in eZee after the stay left the hourly sync's re-read window, so the change was never pulled. Refreshed both rows from a fresh pull and applied the standard reprice (logged 'Repriced from EZEE'); both bookings now 172.39 = eZee. Systemic gap for the reconciliation build: charges edited after a stay leaves the sync window are invisible until month-end; compare TotalExtraCharge/TotalPayment per stay daily.
- 18 Sep: hourly eZee sync (hour:update, HistoricalApi.php) had crashed every hour since 17 Sep 00:00 (39 times) on an undefined $extraChargeTax in the Bell Suites branch, so each hourly pull stopped partway. Fixed (commit ef53345); manual run after deploy completed clean (44 assigned, 3 moved, 16 conflicts). Edit Booking save verified live on #68522 (values unchanged).
- 18 Sep: Edit Booking save was broken since 9 Sep (22 failed saves): the 7 Sep duplicate guard read $book before it was loaded; fixed by loading first (commit 33dce48). Side effect fixed too: the locked-month check on updates had been a no-op. Lesson: after touching a controller, exercise the form once on the live site.
- **NEXT BUILD (Sam, 18 Sep): daily accuracy reconciliation, not just alerts.** (1) Every morning compare each eZee reservation of the last 30 days with its Homemoka booking on unit, dates, room amount, cleaning, total; (2) show the count on the admin dashboard and in a 7am email ("N stays, N match, N mismatches, N missing") with every mismatch listed; (3) every mismatch raises a Needs Review item automatically; (4) weekly folio-level comparison against the eZee revenue report uploaded through the existing Upload page, listing folios eZee has that Homemoka does not (catches after-night-audit stays with no RES, rule 26); (5) month-end gate: no statements until the month shows zero unexplained mismatches; (6) job-failure alert as one line of the same email (a crash must never again be silent). About two days. Sam wants other bugs solved first; do not start without his go.
- 18 Sep decisions (Sam): checklist A = all 10 cancelled (left as is); B = no-shows count as cancelled (left as is); C = 6 clashes for the team on Needs Review; J-15-06 FN8806 pieces Nov 2025–Jul 2026 changed Website→Long Term Rental, ota_fee 0 (DataLog 'Fee source change'); FN32646 H-03-06 parked as a placeholder eZee row (status 7, never auto-assigned) with a Needs Review item for staff to key the real stay. FN6724 Forum Extra Room = company revenue, optional to key.
- 18 Sep late: second and third restore passes (27 no-room future reservations retired by the sweep; 21 checked-in/out stays marked gone with no cancel event) — all logged as DataLog 'EZEE sweep restore'. Team list of 178: 166 assigned. Staff checklist ~/Documents/Staff-check-18-Sep-2026.xlsx: A = 10 with eZee cancel events (confirm), B = 8 cancelled here but Confirmed in eZee, C = 6 Needs Review clashes, D = FN32646 to key, FN6724 Forum Extra Room, J-15-06 fee source question. Open question: what set the 21 to status 1 around 23:00 on 12–18 Sep (not the sweep, not a Cancel notification) — check EzeeNotifications/hour:update before re-enabling any sweep.
- 18 Sep 2026, second finding: while auto-assign was down, the 05:00 ezee:sweep-cancelled retired 112 UNASSIGNED reservations (9–18 Sep) as "absent from the eZee booking list"; only 1 has an eZee cancellation event, 27 were stays eZee later reported checked in/out, and eZee's Booking list also omits live monthly tenancies (dry run flags 70 assigned bookings as "cancelled"). Sweep schedule PAUSED in Kernel.php until the pull is proven complete. Checklist for Afiq: ~/Documents/Sweep-retired-check-Sep-2026.xlsx (sheet 1 = retired rows to confirm; sheet 2 = the 70 flagged). 18 Sep evening: Sam confirmed in eZee that only RES6536 (Forum SS-22-05) was truly cancelled; the other 110 were restored (status 1→5, DataLog 'EZEE sweep restore' + assignment log per row) and ezee:auto-assign --from 2026-08-19 placed 105; 5 sit in Needs Review as clashes (RES6582, RES6588, RES6611, RES31367, RES31372). Sheet 2 of the checklist is moot: eZee's list omitted live RES31133.
- 18 Sep 2026 incident: the daily ezee:auto-assign aborted on every run from 10 Sep 23:00 to 18 Sep because a rule-25 review note exceeded ezee_assignment_logs.note varchar(255) (Sam noticed J-08-12 empty for September). Fixed: note is TEXT (migration 2026_09_18), model caps notes; guest creation no longer dies on a missing or long eZee country (Guests.php). Re-ran from 1 Sep: 230 stays assigned, 14 conflicts to Needs Review, 0 failed. Lesson: the run must not abort on one row; and the daily job needs an alert when it fails (see to-do).
- DDoS / bill protection (Sam, 16 Sep): (1) Google Cloud budget alert on the project; (2) nginx global per-address request limit; (3) Cloudflare free tier in front of homemoka.com (nameserver change at GoDaddy with Sam present; hides the VM IP and absorbs floods). Do 1 and 2 first, they cost nothing.
- Weekly blog: cloud routine "MOKA weekly blog drafts" (trig_015KZ1PMcCbcqkv4D3fqdDe5) runs Mondays 08:00 MYT, drafts two posts per docs/BLOG-WEEKLY.md onto a blog/drafts-YYYY-MM-DD branch. Sam approves, then the moka-blog-publish skill merges, deploys and requests indexing. First drafts (16 Sep): branch blog/drafts-2026-09-16 (roundup, F1 Sepang pricing guide, MySTRA community post).
- Billing: the linked account 011705 lapsed on 17 Sep 2026 and Google cut the VM off the network (site down ~hours); it is enabled again as of 18 Sep 03:40 UTC. Sam cannot see that account under his login. If it lapses again: link the project to Sam's own account 010E87-FE4B61-CD906B (`gcloud billing projects link moka-staging-494606 --billing-account 010E87-FE4B61-CD906B`), with his yes. Done 18 Sep once billing returned: firewall is now allow-ssh-iap-only (tcp:22 from 35.235.240.0/20) plus http/https/icmp/internal; default-allow-ssh, allow-8080, default-allow-rdp deleted; **every gcloud compute ssh/scp must use `--tunnel-through-iap`**. Bucket uploads work again (nightly job uploaded 17 and 18 Sep on its own; 9–14 Sep dumps re-uploaded from the Mac).
- Security next: two-factor login for the 23 admin accounts (~1 day); Cloudflare free tier in front of the site (needs
  nameserver move at GoDaddy with Sam present); Laravel 9 is past security support, plan the upgrade.
  Done 11 Sep: fail2ban installed, security updates applied + reboot, VM deletion protection on, HSTS + security headers.
- Enter the Zoho app password (org policy currently blocks app passwords; owner is the Zoho admin) and send a test email.
- Restore drill of a nightly dump. Close SSH port 22 to the internet (use IAP). Rolling --from for the 06:00 job.
- Needs Review holds only same-week room clashes; the front desk clears them from the eZee calendar.
- Roadmap page (artifact a4628199-ffcd-4743-89f3-05525b4fec23) and staff playbook (artifact 3d24b342-41f7-405c-abcd-86b354ac8de2).

## August data audit — final (4 Sep 2026, evening)

Checked all 1,040 eZee-linked August bookings against the five eZee revenue reports and the extra-charge files.
Result: 1,038 exact; the 2 left are split-stay pieces (RES6075, RES6317) where the cleaning fee sits on the second
piece of the same folio, so each stay's total still matches eZee. 50 hand-keyed bookings have no eZee line to compare.

Rules learned (all applied, logged in storage/logs/price-repair-2026-09-04.csv):
- eZee folio numbers repeat across hotels (FN4140 exists at Alinea and Arte) — always key by hotel + folio.
- Alinea folio numbers are not the TransactionId suffix; Forum/Bell/Eko/Arte are. Six July stays carried a
  server_folio_no that belonged to a later booking; cleared (Forum/Bell) or corrected from the report (RES3947 → FN4101).
- Cleaning fee = eZee "Cleaning Fee*" line, else the "Channel" line; damage deposits (RES31053) and late check-out
  charges (RES6436) are never cleaning. Where the eZee report bills room only, MOKA cleaning is 0.
- SST(CF) only where eZee taxed the cleaning line (Agoda "channel" lines carry none).

## Ground rules confirmed and enforced (4 Sep 2026, night)

Sam confirmed docs/GROUND-RULES.md. Enforced in one place: app/Support/Channel.php (channel list, fee groups) used by
EzeePricing (fee), EzeeAutoAssign (stamped source), both exports; admin sidebar "Ground rules" renders the doc.
August data aligned to the rules (all logged in storage/logs/price-repair-2026-09-04.csv):
- 565 rows renamed to the channel list; fee corrected on 388 rows (Website = 8% of room before tax; LTR, Agoda, Trip.com, Tiket.com = 0).
- 4 rows re-sourced from eZee's report (2 LTR, 1 Booking.com, 1 Owner); 8 guest names refreshed; 2 stale folios fixed.
- 10 tenancies eZee reports as Long Term Rental (all their month pieces) set to LTR / 0 fee; a wrong pass that relabelled
  18 tenancy pieces as Website was reverted the same night (log rows U-channel-from-sibling / U-revert-sibling-channel).
- Cleaning moved to the first piece on RES6075 and RES6317. Room-move stays keep cleaning on the unit the guest ended in.
- Old export: a hand-keyed month/split piece prints the RES, folio, guest and channel of its linked sibling
  (same MOKA folio, same room or touching dates).
Final audit of the generated August export: 1,090 rows, 1,085 carry an eZee RES, all 1,085 match eZee on rate, cleaning,
SST, SST(CF), channel, fee rule and total. 5 rows are MOKA-only (no eZee stay).
Known limits: tenancies from 2024/2025 are not in eZee's August revenue report, so their rent is as keyed by staff.

## Edit Booking: Reassign and Split (4 Sep 2026, late)
Edit Booking (/admin/book/{id}/edit) now has a "Unit" card: every piece of the stay, Reassign (POST /admin/booking/{id}/reassign,
overlap-checked on save, logged) and Split (existing POST /admin/booking/{id}/split via BookingSplitter). Same server code as Needs Review.
Voided eZee reservations: eZee sends no cancel event for a void; only the weekly sweep (Mon 05:00) notices the absence.
Proposed to Sam: run ezee:sweep-cancelled daily (default retires unassigned only; assigned go to review). Awaiting his go.

## Booking actions, one set everywhere (4 Sep 2026, night)
Actions (all POST JSON, EzeeRoomMappingController): /admin/booking/{id}/reassign, /split, /swap, /cancel; /admin/ezee/booking/{ezeeId}/voided.
Cancel and Voided never delete: booking status 1, unit freed, eZee record status 1 (never re-created), reason + staff logged.
Reachable from: Edit Booking (Unit & room moves card, #unit-card), Booking detail (Cancel, Unit / room move), Bookings list
(Cancel per row; Delete only with bookings.delete), Needs Review row (Open #id, Voided in eZee), Edit EZEE Booking
(Open booking, Voided in eZee), Listing calendar popup (Open booking, Unit / room move).
Still open for Sam: daily sweep, stamp rule (automation proposes, humans apply), source dropdown to canonical list.

## 7 Sep 2026: split flag, tax-field bug, regression guard
- bookings.is_split (migration 2026_09_07): set by BookingSplitter on both pieces, or ticked on Edit Booking. Exports print
  "Split Booking" first in Remarks when is_split, or the remark mentions split. Flagged by Sam: #66915, #67127, #67208, #67768.
- Bug found by the baseline diff: EzeePricing read AmountAfterTax but eZee's charge lines carry Amount, so SST on cleaning
  computed as 0 for every notification-sourced stay; the hourly reprice had zeroed 42 August rows after 4 Sep 23:00.
  Fixed (Amount fallback), extra_charge_tax recomputed on 341 eZee rows, 343 linked stays repriced through acceptEzeeAmounts.
- Regression guard: scratchpad aug_export_baseline.xlsx = the audited 4 Sep export. After any change: regenerate August on the
  server (/tmp/exp.php), scp, diff by Booking Id; every differing cell must be explained before deploy is called done.

## 7 Sep 2026, later: sweep bug, room swaps
- The 05:00 sweep retired two real stays (RES6381 Forum SS-09-02 4-6 Sep, RES3471 Alinea 11-08 3-5 Sep): eZee's booking list
  drops checked-out stays, so absence looked like a void. Fixed: the sweep never retires a stay that has started or ended
  (Checked Out/In, Stayover, Due Out, or Start before today) and now logs every retirement. Both stays restored (#68536, #68537).
- Ground rule 20: room swaps eZee confirms are applied by the sync for unlocked months and raised for review. The sync never
  overrides a placement a person made by hand (staff use Swap on Edit Booking for those).
- RES31086 FN32414 is a day-use (Start = End, RM 27.78): MOKA stores 0 nights, rate 0. Open: how day-use should print.

## Future plan: hourly / day-use bookings (raised 7 Sep 2026)
eZee day-use = Start equals End (e.g. RES31086 FN32414, 27 Aug, RM 27.78). MOKA stores 0 nights, rate 0, total 0: the revenue
is lost from exports and owner statements. Proposed recording rule, to confirm with Sam: keep 0 nights (no calendar night
taken), put the room charge in the Total with SST, print "Day use" in Remarks, rate column blank; owner report shows it as a
day-use line. Until decided, day-use rows show zero and should be checked by hand at month end.

## Locked 7 Sep 2026 (v2026.09.07-locked)
Sam checked the regenerated August export and locked this version. Baseline for the regression guard:
docs/baselines/august-2026-export-baseline.xlsx (1,091 rows; 1,087 eZee-linked, all matching) with the audit script beside it.
Rule for future work: regenerate August, diff against this baseline, explain every differing cell before a deploy is done.
Still open: cleaning on a no-show first night (RES6289), day-use recording, sweep/stamp questions listed above.

## Locked 18 Sep 2026 (v2026.09.18-locked)
- eZee sync repaired: assignment-log note is TEXT (long rule-25 notes had aborted every daily run 10–18 Sep), guests with no or long country save, hourly sync's Bell Suites branch computes extra-charge tax. 230 September stays re-assigned; 158 reservations the sweep or the outage had wrongly retired were restored on Sam's confirmation (only RES6536 was a real cancellation). Daily cancellation sweep PAUSED: absence from eZee's booking list is not a void.
- Edit Booking: save works again (booking loaded before the lock and duplicate checks; locked-month check on updates is enforced again); whole-stay banner for monthly pieces; overlap and sibling-piece messages name the booking and never tell staff to cancel a piece.
- Guests: a booking saved without phone and email no longer attaches to or renames the shared account #10; 953 eZee-linked bookings and 13 split pieces re-attached to real guests; #10 shows "Guest (name not recorded)".
- Data corrections, all logged: RES6522 and 12 September cleaning fees to eZee's figures; Sarah Atikah RES4160 (G-12-03 extended to 13 Sep, cleaning moved there); J-15-06 tenancy to Long Term Rental; FN32646 parked on Needs Review. Ground rule 26 (folio-only transactions after night audit).
- Site: three blog posts + weekly drafting routine and publish skill, unique photo library, Services and About images as WebP, old-site URLs 301, /home to homepage, sitemap resubmitted. Server: SSH through IAP only, open SSH/8080/RDP rules deleted, bucket backups working again.
- Next build, agreed: error alert, deploy smoke test, daily eZee reconciliation with split check.

## Locked 14 Sep 2026 (v2026.09.14-locked)
- Public forms hardened after spam estimate enquiries: honeypot field, throttle 10/min on /estimate, /contact, /subscribe,
  duplicate estimate (same email or phone within ten minutes) neither stored nor emailed, owner-only notice with the guest
  link to staymoka.com, name field and submit-disable on the homepage estimate form.
- Launch checklist fixes: custom 404 on the site template, one H1 per page, titles ≤60 and descriptions tightened, alt text on
  estimate icons; checklist saved as the site-launch-check skill.
- Legal: Privacy notice lists exactly which cookies the site sets; Terms and Designs page no longer claim an Innspace trademark;
  current phone number on both legal pages.
- Server (not in the repo): HSTS + security headers snippet in every nginx location, fail2ban on SSH, security updates applied
  with reboot, VM deletion protection on, IAP tunnel SSH tested.
- Open first: Google Cloud billing is delinquent, so bucket uploads have failed since 8 Sep and the firewall change is refused.

## Locked 11 Sep 2026 (v2026.09.11-locked)
- Designs page (Innspace): official SkyWorld/Solution+ logos, featured showcase with filmstrip (The Valley, SkyVogue,
  Sky Awani 4), project index, handyman section, no page auto-scroll, photo protection (no drag/right-click; nginx hotlink
  block + rate limit on /new-theme23/images/projects/).
- Blog: index with featured post, topic filters and photo cards; article layout with cover, contents, figures, share,
  author; posts carry topic + photo in config/blog.php. Three new articles (Penang by-laws, JMB/authorities, Early Bird).
- SEO: homepage at / only (/homepage 301), www/trailing-slash/index.php 301 to clean URLs (nginx canonical-host block),
  sitemap lastmod for static pages, five city landing pages in config/solutions.php, solutions index "Where we operate",
  solution page template without photos, footer Solutions column trimmed to audiences, TikTok + address in schema.
- Privacy and Terms on the site template with contents; footer "Moka Venture Sdn Bhd", MOKA® trademark and impersonation
  notice; Innspace mark note on Designs; Terms IP clause names the marks.
- Owner booking page shows room revenue, (-) M&A fee, net to owner only.

## Locked 10 Sep 2026 (v2026.09.10-locked)
- Rule 25: stays parked on Extra Rooms raise Needs Review items, never moved (EzeeAutoAssign). August fixes applied by
  Split/Restore for RES3413, RES30960-1/-2/-3, RES30720 tail, RES29951 tail (see 10 Sep section). Pending: RES20622,
  16 other Extra Room stays for Afiq's Room Charges check.
- Owner booking page shows only the owner's side: price per night, room revenue, (-) M&A fee, net to owner. No SST,
  cleaning or tax on cleaning (company revenue). Open question: rule 23 pool amount still includes cleaning.
- Owner portal phone app frame (top bar, bottom tabs, account sheet); app log-in screen from the home-screen icon
  (/login?app=1, moka_app cookie); install hint after sign-in with permanent dismiss.
- Blog: Penang STR by-laws 2026, working with building management, SkyWorld Solution+ Early Bird; indexing requested.

## 10 Sep 2026: Extra Room parking (rule 25)
- Cause found for "missing" August revenue on Alinea 23-11, J-27-11, J-26-02, H-09-21 and others: eZee reports only the last
  room, and the front desk parks stays on "Extra Room N" after the unit nights. On 3 Sep the sync moved 7 stays into company
  rooms (guard added the same day); other stays were created straight onto Extra Rooms.
- Fixed by Split/Restore at Sam's instruction after Afiq read eZee Room Charges: RES3413 (23-11), RES30960-1/-2/-3
  (J-27-11, H-05-06, J-26-02), RES30720 tail (H-09-21), RES29951 tail (J-27-11). Totals unchanged; tails on Extra Room 1 are
  company revenue. Pending: RES20622 Bell Suites 03-09; 16 other August stays on Extra Rooms need the same Room Charges check.
- Rule 25 in code (EzeeAutoAssign): review items instead of silent moves/creations on Extra Rooms.

## Locked 9 Sep 2026, afternoon (v2026.09.09b-locked)
- Owner portal as a home-screen app, built: manifest.json (start /home), green icon set in public/images/app, iPhone tags
  (partials/pwa.blade.php in owner + newTheme layouts), minimal service worker (public/sw.js) with offline.html, install
  hint (partials/pwa-install.blade.php) on the owner portal after sign-in: Android Install button, iPhone Share steps,
  permanent "Not now" / "Already added", hidden when running installed and while typing.
- Login page on phones: shorter hero so the form sits above the keyboard; chat widget and install hint hide while typing.
- Site figures updated (200,000+ trips, RM30 Million+ for owners); About/Services carousel and tab fixes; homepage
  picture wrapper fix; Solutions redesign live (all in v2026.09.09-locked earlier today).

## Locked 9 Sep 2026 (v2026.09.09-locked)
- Solutions index (/solutions) redesigned and live: hero with trust strip, "who you are" tiles, "how it earns" cards,
  comparison table, renovation band, three steps, orange estimate band. Styles: new-theme23/css/solutions-index23.css.
  /solutions/preview now 301s to /solutions.
- Fixed pages broken since the 31 Aug front-end rebuild: About portfolio + review carousels (start-up script restored
  in partials/carousels.blade.php), Services dashboard image tabs and the shared layout script (guarded), homepage
  dashboard phone image (picture { display: contents } in all23.css, link now versioned).
- Site figures: 200,000+ trips hosted, RM30 Million+ revenue earned for owners (homepage, services, about, templates).
- Login pages say Owners; login flow polish (password show/hide, enter-key hints, chat widget hides while typing).
- Future plan recorded: owner portal as a home-screen app (manifest, icons, service worker, install hint).

## Locked 8 Sep 2026, evening (v2026.09.08b-locked)
- Website: SEO (Search Console verified, sitemap, 9 solution pages, old-site 301s), speed (gzip, static caching, WOFF2, WebP,
  lazy loading, cached home query), booking engine retired (301 to staymoka.com), mail via Zoho (admin /settings/mail),
  estimate/contact/subscribe/reset emails, respond.io widget on every page.
- One header and one footer on every page. Header on scroll: original pages stay orange (their own script), green pages stay
  green (shared script + phone rule in blog23.css). Footer: socials above the rule, tagline "Malaysia's #1 property
  management company".
- Login, forgot-password and new-password pages rebuilt on the homepage template (account23.css). Every Log In link goes to
  /login; the old login pop-up is no longer linked (markup still in the original page files). Phone menu shows an
  "Owner log in" pill; password show/hide; enter-key hints; chat widget hides while typing on phones.

## Locked 8 Sep 2026 (v2026.09.08-locked)
- Ground rule 23: pool profit sharing. Pool = Group of listings (type "group"); owners see pool totals (room + cleaning, before SST,
  M&A not deducted) on the dashboard and report page; pool weights stored on listings (pool_weight, by unit size) for statements.
  Calendar stays per unit. Helper: app/Support/Pool.php.
- Owner portal redesign: dashboard (KPI cards, donuts with legends, trends), calendar (channel colours from Channel::colours(),
  today in brand green, legend, month stats, one-row-per-booking list view), My Units cards. Day-use stays hidden on calendars.
- Owner report page grouped queries made strict-mode safe.

## 8 Sep 2026: guest profiles (rule 24)
app/Support/Guests.php builds one profile per guest from eZee (email, then mobile, then name-only when no contact on either
side); EzeeAutoAssign::create and both manual-assign paths in BookController use it. Cleanup run 8 Sep: 48,111 numbered names
cleaned ("NUR56" -> "NUR"); duplicates merged by email and mobile: 12,005 bookings repointed to 1,645 + 3,329 kept profiles,
logged in storage/logs/guest-merge-2026-09-08.csv (booking_id, old_user, new_user, key). Duplicate user rows are left in place
(unreferenced), not deleted.

## 8 Sep 2026, evening: SEO, speed, booking engine, mail
- Search Console: https://homemoka.com verified for ss.samkong@gmail.com (file public/googleab79fdbd400cb9b8.html, keep it); sitemap resubmitted.
- Nine solution landing pages (config/solutions.php, v2.pages.solution) at /solutions/{slug}; old-site paths 301 to new pages.
- Speed: nginx gzip for css/js/fonts (conf.d/moka-perf.conf), static caching (7d css/js, 1y fonts/images), HTTP/2; WOFF2 fonts
  with font-display swap; WebP for the heavy new-theme23 images via <picture>; lazy-loading below the first image.
- Booking engine retired: /location/search and /listing/{key} 301 to https://staymoka.com; unit pages out of the sitemap.
- Mail: admin page /admin/settings/mail (super admin) stores the SMTP account encrypted in app_settings and overrides .env at
  boot (AppSettings::applyMail). Sam entered the Zoho app password there on 8 Sep and the test email worked. Estimate form
  emails hello@homemoka.com (App\Mail\EstimateRequested).
- Header Log In falls back to /login on pages without the pop-up (blog).

## Future plan (added 8 Sep 2026)
- Newsletter tool: welcome email to new subscribers, campaigns, unsubscribe handling and consent record (PDPA). Subscribers
  are saved in the `subscribe` table (61 as of 8 Sep) and each new one emails hello@homemoka.com; nothing is sent to them yet.
- Guest marketing (email/WhatsApp) from the guest database needs the same consent capture at check-in.
- Owner portal as a home-screen app: BUILT 9 Sep 2026. public/manifest.json (start /home, green theme), icons in
  public/images/app (from the logo, on #004a49; maskable variants), partials/pwa.blade.php (manifest + iPhone tags, in the
  owner and newTheme layouts before the favicon), public/sw.js (installability + offline.html only; portal pages never
  cached), partials/pwa-install.blade.php (phone-only hint on the owner portal after sign-in, not on /login: Android Install
  button via beforeinstallprompt, iPhone Share > Add to Home Screen steps; hidden when running installed and
  permanently after "Not now" / "Already added", since iPhone Safari cannot tell whether the app is installed;
  test with ?a2hs=ios / ?a2hs=android). Push notifications for new bookings remain a later step.
- Owner portal phone app frame (9 Sep 2026, owner/layout.blade.php, <=768px): fixed top bar (logo + page title from
  @section('title')), fixed bottom tabs Dashboard / Units / Calendar / Account, Account opens a bottom sheet (name, email,
  Change password, Log out); sidebar, hamburger bar and page h1 hidden on phones; safe-area padding (viewport-fit=cover);
  install hint sits above the tabs. Tablets 769-1024 keep the hamburger sidebar; desktop unchanged.
